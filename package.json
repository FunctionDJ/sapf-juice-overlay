{
  "type": "module",
  "dependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.0",
    "motion": "^12.38.0",
    "p5": "^2.2.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.0",
    "vite": "^7.3.1"
  },
  "scripts": {
    "dev": "bun mock-api.ts & vite",
    "build": "vite build && rm dist/*.png && rm -f dist.zip && zip -r dist.zip dist/*",
    "check-lint-build": "bunx tsc && bunx eslint . && vite build"
  }
}